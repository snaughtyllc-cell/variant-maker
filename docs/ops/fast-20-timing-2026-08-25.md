# Live Fast 20 timing — 2026-08-25

One timed pack on **live** Fast. No live PATCH. No `VF_LAB`. Used to fill
§1 of `docs/superpowers/specs/2026-08-25-launch-and-unit-economics.md`.

## Setup

| | |
|---|---|
| Endpoint | `j0b1q4iuunzhnq` (`varyforge-fast-cpu`) |
| Image | production digest (same as live pin `856e23d`) |
| Worker | `mu4rioyvxk8vi6` |
| Job | `3e79485c-ba95-47df-9987-6d2d6aa5de37-u2` |
| Submitted | 2026-08-25T02:58:21Z |
| Terminal | 2026-08-25T03:58:43Z |
| Clip | `portrait.mp4` — 720×1280, 22.063s, 30fps (~2.1 MB) |
| Source key | `inputs/timing20_2eaed792/portrait.mp4` |
| Payload | count 20, preset/platform defaults, `quality_mode=fast`, `jobs=8`, `allow_creative_escalate=true`, autotune on |

Health at submit: 2 idle / 2 ready / 0 in progress (already warm; **not** a
cold start). `delayTime` 842 ms.

## Result

```
status          FAILED
error           executionTimeout exceeded
delayTime       842
executionTime   3612120   # ms = 60.20 min
delivered       0
```

RunPod `output` was empty (timeout). Gallery/R2 copy-back never ran.

## On-worker progress (SSE)

`jobs=8` so copies run in waves of 8. Autotune `max_iters=5` then one
creative escalate → **6 renders per copy** on this clip (gate never cleared
on autotune).

| Copies | First seen | Last state | Wall |
|---|---|---|---|
| 1–8 | rendering @ ~6s | **done** @ 1473–1502s (~24.9 min) | 6 renders + escalate each |
| 9–16 | rendering @ 1473–1504s | **uniqueness** after escalate @ 3591–3609s | timed out |
| 17–20 | — | never started | — |

Compressed copy 1: render → check → uniq → (×4 more autotune renders) →
escalating → render → check → uniq → **done** @ 1493s.

Copy 9 (wave 2) followed the same ladder; uniqueness dwell after a miss
stretched from ~14s (wave 1) to ~160s (vs the 8 already-kept peers). Wave 2
was scoring the escalate encode when the 3600s cap hit.

Progress-event counts: rendering 95, checking 96, uniqueness 96,
escalating 16, done 8.

## COGS at $0.40/hr (`cpu3g-8-32`)

| | Seconds | $ |
|---|---|---|
| Billed encode (`executionTime`) | 3612 | $0.40 |
| Live idle if this worker then sat 600s | 600 | $0.07 |
| **This failed 20-pack** | | **~$0.47** |
| Wave 1 only (8 `done`, if the job had returned) | 1493 | $0.17 encode + idle |

A second Fast CPU was already idle at submit (max workers 2). That occupancy
is not this pack’s encode bill.

## Do not from this run

- Do not PATCH live Fast (timeout, idle, image, `VF_LAB`).
- Do not raise the 24/24 uniqueness gate to make 20-packs cheaper.
- Do not treat this as a cold-start number.
