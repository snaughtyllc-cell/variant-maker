# Live Fast pin — `7dae269` — 2026-08-25

Jeff signed lab Fast 8 look, then lab Fast 20 of the same 720 SaveInta
clip completed in 13.4 min. Live Fast `j0b1q4iuunzhnq` is now digest-pinned
to that engine. **No `VF_LAB` on live.** Lab stays `VF_LAB=1` on the same
digest.

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:5f815e72eba0b32b100943b6ea4546992149a073a3a421c8fddb740f59f6fc4e` |
| `VF_ENGINE_REV` | `7dae269` |
| `VF_LAB` | unset |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |

Prior live pin: `sha256:59caa472…` / `856e23d` (caption-safe 1080 crop).

Live smoke (same 720×1280 22s SaveInta, after recycle):

- Job `5f23361e-a5cb-4f31-be6e-019706aca07c-u1`
- **8/8** medium, 0 escalate, **25–26 bits**, VMAF 94.8–98.5
- Crop keep **0.87–0.90**, y **0.92–0.98** (720 talking-head from the top, not 0.92–0.96 center)
- Worker **251 s (4.2 min)**, drop-to-files **267 s**, first progress **13.3 s** (delay 8.9 s)

Superseded 2026-08-26: live Fast is `472ab60` / `sha256:e7474975…` (look stills overlap uniqueness). Writeup: `docs/ops/live-pin-472ab60-2026-08-26.md`.

Do **not** re-pin live to a raw `:latest` until that build is tested on lab.

Team capacity (2026-08-26): live Fast max **4** (CPU, default Generate). HQ
`f0carwe6u9bdd6` max **3** (4090/L40S). Min workers stay **0** — overnight $0.
AQMTp-class nose-close uniqueness is parked; most uploads are not that crop.
Testers use **live** Fast. Look stills shipped in `472ab60`; do not pin AQMTp uniqueness or shade.

