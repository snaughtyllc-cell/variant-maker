# Live Fast pin — `c497505` — 2026-08-29

Handheld crop wander (start→end pan + two-sine wander) stacked on compete
axes. Jeff: **ok lets build that on live.** **No `VF_LAB` on live.** Lab Fast
is also on this digest (`VF_LAB=1`) for the verify pack. **Lab verify
`1fbe4f51de83` done — live stays. Do not roll back to `d0a7bc5`.**

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:3d24473a35c4c624ee1c90308ff15d78ea95c178f54798a7a532f258b11694ef` |
| `VF_ENGINE_REV` | `c497505` |
| `VF_LAB` | unset |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |
| Studio | production health `lab` unset; Generate uses live Fast |

Prior live pin: `sha256:a5b703fa…` / `d0a7bc5` (compete rotate / vignette / 30/48/60).

## What shipped

Port of PR #56 feat commits (`23862e8` / `bc88da1`) onto current `tier1` so
compete axes stay. Separate RNG (`seed ^ 0xC0DE5`) so trim / resample /
vignette / `out_fps` stay seed-stable. Gate **24/24**. 19-bit ship floor
unchanged. AQMTp-class tight face stays parked.

| Axis | Rule |
|---|---|
| X travel floor | talking-head **≥ 0.08**, else **≥ 0.10** |
| Max \|end−start\| | TH **0.24**, else **0.28** |
| Ease | smoothstep + 2× scale around crop |
| 720 y | start/end ± handheld stay in **0.90–1.00** |
| VMAF | strips ends + handheld amps |

## Pin

Live Fast `j0b1q4iuunzhnq` patched to the new digest, `VF_ENGINE_REV=c497505`,
**no `VF_LAB`**. Workers drained (max 0) then restored to max **4** / idle
**600**. Lab Fast left at max **1** / idle **120** / `VF_LAB=1` / compete
digest. Production Studio endpoint id unchanged.

First Generate after recycle may cold-start. Do not inject LOOK packs into
live tenant `ws_87443c06e3`. Jeff’s stills on the next real pack are the look
oracle.

## Lab verify (2026-08-29) — stay

Same three clips as compete LOOK `166cf4bae4be`, lab tenant only
(`1fbe4f51de83`). Fast 2, escalate on. Lab Fast pinned to this digest +
`VF_LAB=1` for the run.

| Clip | Compete LOOK | Drift lab |
|---|---|---|
| SaveInta | 33/32 `ok`, look MAE fail/fail | **33/33** `ok`, look ok/fail |
| AQMTp (parked) | 20/19 `ok` / `below_target` | **18/17** `uniqueness_fail` |
| bring-me-down | 45/46 `ok` | **43/45** `ok` |

SaveInta/motion stayed in the compete bit band. Stills: no lava, snow, hard
pops, or eaten captions. AQMTp 2-bit dip is the parked nose-close hole — not
a pin blocker. Writeup: `docs/ops/lab-verify-c497505-2026-08-29.md`.

**Live Fast left on `c497505` / `sha256:3d24473a…` / no `VF_LAB` / max 4.**
