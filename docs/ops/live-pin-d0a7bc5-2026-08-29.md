# Live Fast pin — `d0a7bc5` — 2026-08-29

Compete axes (rotate, vignette, per-copy 30/48/60 fps). Jeff signed the
lab `LOOK-*` pack, then parked AQMTp escalate-on under-19 as a pin
blocker (unusual nose-close face). **No `VF_LAB` on live.** Lab stays
`VF_LAB=1` on the same digest.

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:a5b703fa999d2fa51122eb7d549291db396e17c536f73aa6c6f508194911e520` |
| `VF_ENGINE_REV` | `d0a7bc5` |
| `VF_LAB` | unset |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |
| Studio | production health `{"status":"ok","lab":false}` |

Prior live pin: `sha256:00564ea3…` / `f05d803` (19-bit / 30% ship floor).

## Look Sign

Lab Gallery `166cf4bae4be` — uniquely named so it sat at the top:

| Card | Bits | Notes |
|---|---|---|
| `LOOK-SaveInta.mp4` | **33 / 32** | medium |
| `LOOK-AQMTp.mp4` | **20 / 19** | floor ships |
| `LOOK-bring-me-down.mp4` | **45 / 46** | live daily motion |

Jeff: **yea they look fine.** Average daily clips good.

AQMTp escalate-on `9ba1af34f29d` was **18 / 17** `uniqueness_fail`. Jeff:
that clip is super-close face-to-camera, unlikely on average uploads.
Do not build an escalate-rollback just for it. Do not raise 24. Do not
redraw shade.

## Pin

Live Fast `j0b1q4iuunzhnq` patched to the compete digest, `VF_ENGINE_REV=d0a7bc5`,
**no `VF_LAB`**. Workers drained (max 0) then restored to max **4** / idle
**600**. Lab Fast left at max **1** / idle **120** / `VF_LAB=1`.
Production Studio endpoint id unchanged.
