# Live Fast pin — `472ab60` — 2026-08-26

Visual phase: source vs first-result stills on Generate, without extra
wait. Lab SaveInta Fast 8 on this digest was **faster** than the prior
live uniqueness-only 8. **No `VF_LAB` on live.** Lab stays `VF_LAB=1`.

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:e747497533c4ca53fcce03d7ae0d287de3029a8edbd3298c33c1ceb885bf6e85` |
| `VF_ENGINE_REV` | `472ab60` |
| `VF_LAB` | unset |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |
| Studio | looking UI on production; health `{"status":"ok","lab":false}` |

Prior live pin: `sha256:5f815e72…` / `7dae269` (IG-720 Fast 20 wait-time).

## Proof it does not add Generate wait

Stills are two 360px JPEGs overlapping uniqueness SSIM. Coarse MAE runs
**after** uniqueness (running it beside 8-wide SSIM stretched the pack).

| Pack | Worker | Bits | Looking vs uniqueness |
|---|---|---|---|
| Live SaveInta Fast 8 `7dae269` (prior) | **251 s (4.2 min)** warm | 25–26, all medium | no stills |
| Lab SaveInta Fast 8 MAE∥SSIM `8fe2d26` | **514 s (8.6 min)** | 25–26, all medium | stills on card; uniqueness phase stretched |
| Lab SaveInta Fast 8 `472ab60` (MAE after) | **188 s (3.1 min)** warm | 25–27, all medium | `looking` then `uniqueness` (~0–2 s poll) |
| Live SaveInta Fast 2 smoke `472ab60` | **90 s** | 25–26, medium | `looking` then `uniqueness` |

Local 22s SaveInta: stills ~0.2 s, MAE ~0.6 s, uniqueness ~5 s.

Gate stays **24/24**. Shade stays off. AQMTp uniqueness remains parked.

MAE can trip on signed crop even when stills look like the source. The
stills are the look check, not the red/green. Do not raise the uniqueness
gate to “fix” MAE.
