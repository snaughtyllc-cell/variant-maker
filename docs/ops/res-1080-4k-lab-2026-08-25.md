# 1080 + iPhone 4K (Studio path) — lab Fast — 2026-08-25

Jeff dropped three clips in Lab Studio (count 1, then cancel after
upload). Job `6ac8681df927`. Lab Fast `7dae269` / `sha256:5f815e72…`.
Live Fast was not used.

Studio **always proxies long-edge > 1920 to 1080** before Fast. The
iPhone 4K file (`IMG_0683.MOV`, 214 MB HEVC) became
`IMG_0683_proxy.mp4` (1080×1920, 16.5 s, 59.94 fps, 8-bit). The original
4K bytes were deleted after the proxy passed. Testers on Studio never
hit a native 4K encode.

| Clip | What it is | Size | Dur | Notes |
|---|---|---|---|---|
| `IMG_0683` | iPhone 4K gym pull-up | **1080×1920** after proxy | 16.5 s @ 60 fps | HLG tags kept (`bt2020` / `arib-std-b67`). Classifier called it `talking_head` (self-bits 10) — it is motion. 1080 is **not** phone canvas, so crop stayed caption-safe **0.92–0.96 / y 0.35–0.65**, not the 720 TH punch. |
| `1080a` (`1b7b6c47…mov`) | Native iPhone 1080 talking-head (hoodie) | 1080×1920 HEVC 8-bit bt709 | 7.0 s @ 30 fps | Classifier: `motion` (self-bits 32). |
| SnapTik HD | TikTok 1080 download, pool gag + caption | 1080×1920 10-bit HLG → 8-bit proxy | 6.0 s | Watermarked. Not a look sign-off. |

First-pass (`scripts/first_pass_screen.py`, medium, seed 42): **3 / 3**
clear 24 (51 / 27 / 31 bits).

## Lab Fast (warm worker)

| Pack | Job | Wall | Bits | Preset | VMAF |
|---|---|---|---|---|---|
| Gym 4K→1080 Fast **1** | `9dc5a6c1-…-u1` | **2.8 min** (exec 146 s) | **52** | medium | 100 |
| Gym 4K→1080 Fast **8** | `5df95b6d-…-u2` | **5.4 min** (exec 323 s) | **50–55** | all medium, 0 escalate | 91.8–100 |
| Native 1080 TH Fast **8** | `e974035a-…-u1` | **2.3 min** (exec 133 s) | **26–27** | all medium, 0 escalate | 94.2–99.9 |
| SnapTik 1080 Fast **8** | `62aebd71-…-u2` | **1.7 min** (exec 100 s) | **29–35** | all medium, 0 escalate | 99.8–100 |

1080 Fast 8 of a 7 s talking-head is **faster** than 720 Fast 8 of the
22 s SaveInta clip (5.3 min). The 16 s 60 fps gym pack matches that 720
8-pack wall because one wave of 8 shares the 8 vCPUs.

Crop on every 1080 pack: keep **0.92–0.96**, y **0.36–0.64** (caption-safe).
Gate 24 cleared. No live pin change. No Fast 20 of these clips (not needed
for wait-time; 1080 8-packs already finish in a sitting).

## What this does *not* cover

- **Native 4K through Fast** (no proxy). Studio will not send that.
- AQMTp-class **720** tight face (still 17–21 bits on strong).
- SnapTik is not an IG look test.

Sources kept on R2: `inputs/jeff_res_20260825/`.

Lab Gallery pack: **`look1080th`** (native 1080 hoodie talking-head Fast 8).
