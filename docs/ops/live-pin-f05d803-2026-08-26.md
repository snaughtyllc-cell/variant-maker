# Live Fast pin — `f05d803` — 2026-08-26

19-bit / 30% uniqueness ship floor. Same digest as lab Fast. **No `VF_LAB`
on live.** Lab stays `VF_LAB=1`. Production Studio has the Generate tiles
and uniqueness-miss UI. Jeff: ship the gen view and the floor.

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:00564ea3d284dba82344c764b55ca449949e1c43faa461a216f7392cca04768e` |
| `VF_ENGINE_REV` | `f05d803` |
| `VF_LAB` | unset |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |
| Studio | production Generate towels + miss UI; health `{"status":"ok","lab":false}` |

Prior live pin: `sha256:e7474975…` / `472ab60` (look stills overlap uniqueness).

## Floor (after the 24-bit hunt)

| Score | After hunt | Result |
|---|---|---|
| ≥ 24 | Pass | `ok` |
| 19–23 | Escalate already ran | Ships `below_target` / `status=ok` |
| ≤ 18 | Escalate already ran | `uniqueness_fail` — not Gallery/Drive |

Do not raise `TARGET_BITS` / `MIN_PEER_BITS` (stay 24 / 24). Do not redraw shade.

## Lab prove (same digest)

| Pack | Result |
|---|---|
| SaveInta Fast 2 (`saveintafloor`) | **26/26 bits**, both medium `ok` |
| AQMTp Fast 2 escalate on (`aqmtpfloor`) | medium missed 24 → strong → **19/19 (~30%)** `below_target`, still `ok` |
| AQMTp Fast 2 escalate off (`aqmtpnoesc`) | v01 **16 bits `uniqueness_fail`**; v02 **19 bits `ok` / `below_target`** |

Product Fast keeps escalate on. 19 is not a first-pass skip.

Live workers recycled after the template patch (drain max 0, restore max **4** / idle **600**). Lab Fast left at max **1** / idle **120** / `VF_LAB=1`.
