# Live Fast pin — `fc5a8109` — 2026-09-12

Jeff signed Lab vignette-off (`its fixed`) then: **yes then lets push to
live.** Image-only PATCH. **No `VF_LAB` on live.** Copyid stays **off** on
live. Railway production `RUNPOD_FAST_ENDPOINT_ID` stays the live id.

| | |
|---|---|
| Image | `ghcr.io/snaughtyllc-cell/variant-fast@sha256:fc5a81090bac281b33eae122c087b19d6c9f0c090f8a68a2c1a2686723866097` |
| `VF_ENGINE_REV` | dashboard may stay stale — **digest is the pin** |
| `VF_LAB` | unset |
| Copyid | off |
| Workers | min 0, max **4**, idle **600** |
| Railway | `RUNPOD_FAST_ENDPOINT_ID=j0b1q4iuunzhnq` (unchanged) |
| Studio | production health `lab` unset; Generate uses live Fast |

Prior live pin: `sha256:3d24473a…` / `c497505` (handheld + compete vig).
Jeff: Live gens on that digest were fine. This pin is the Lab-signed skip
(ffmpeg PI/5 made even 0.04 look always-on on the new Lab image).

## What shipped

Same Fast engine Lab is on: identity A–D+C2 (SHA ≠ source/peers, AAC never
copy, empty encoder, no SEI) plus compete vig **0** (skip). Rotate and
per-copy 30/48/60 stay. Gate **24**. No shade. MAE 38 is still a review
trigger, not “realistic.”

PATCH body is `{"image":"…@sha256:fc5a8109…"}` only. Never send `env` —
a body with `env` can wipe live endpoint env. Never set `VF_LAB` on live.

## Pin path

Lab Studio boot (`VARIANT_LAB`) one-shot
`variant_maker/server/live_fast_pin.py` PATCHes **only**
`j0b1q4iuunzhnq`. Production Studio (`lab: false`) does not pin. Lab Fast
CI (`.github/workflows/build-variant-fast-lab.yml`) still PATCHes **lab
only** (`xar25v77v3j27u`). This repo still must not push `:latest`.

If the Studio key is scoped to Lab Fast only, health shows
`live_fast_pin=error_403`. Pin the digest in the RunPod dashboard
(image only; do not add `VF_LAB`). Do not change live max/idle.

First Generate after recycle may cold-start. Warm workers can keep
`c497505` until they cycle. Do not inject LOOK packs into live tenant
`ws_87443c06e3`. Jeff’s stills on the next real pack are the look oracle.

Lab Fast stays `VF_LAB=1`, max **1**, idle **120**, same digest.

## Verified (2026-09-12)

- Lab health after pin: `live_fast_pin=pinned` `v2_patch=200`. After disable:
  `skipped_disabled`. Production stayed `{"status":"ok","lab":false}`.
- GHCR digest `sha256:fc5a8109…3866097` exists (`Docker-Content-Digest` match).
- Unit: image-only PATCH, no `env` / `VF_LAB`, skip when not Lab, never Lab URL.
- Engine: subtle/medium/strong sample vig **0**; dry-run ffmpeg has no
  `vignette=`. Direct Fast render of `tests/fixtures/real_src.mp4` has no
  `vignette=` in the cmd. Same clip with ffmpeg PI/5 (`angle=0.588`) drops
  mean luma **137.17 → 110.69** (Δ **26.48**) — that is the Lab dark wash.
- This VM has no RunPod key, so a live Generate pack was not run here.
  First production Generate may cold-start; warm workers can keep `c497505`
  until they cycle. Jeff’s stills on that pack are the live look oracle.

## After the pin lands

Lab `/api/health` (2026-09-12):
`live_fast_pin=pinned` `live_fast_pin_detail=v2_patch=200`
(and `lab_fast_pin=pinned`). Production `/api/health` stayed
`{"status":"ok","lab":false}`. One-shot is spent
(`ONE_SHOT_ENABLED = False`). Re-pin only with
`VARIANT_LIVE_FAST_PIN=1` or the RunPod dashboard (image only).
