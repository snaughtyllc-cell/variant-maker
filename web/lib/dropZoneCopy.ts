/** Copy for the Studio file picker — works for tap (phone) and drop (desktop). */
export function dropZoneTitle(): string {
  return "Add videos";
}

export function dropZoneSubcopy(): string {
  return "MP4 / MOV — tap to pick from this phone, or drop files. Google Drive is a separate button below.";
}

export function dropZoneHint(): string {
  return "iPhone 4K is fine up to 512 MB (slow upload). We shrink it after it lands. 1080p is faster.";
}

export function dropZoneBrowse(): string {
  return "Browse Camera Roll / Files";
}
